import React from 'react';

function Categories() {
  return (
    <div>
      <button type="submit" className="status">
        Check status
      </button>
    </div>
  );
}

export default Categories;
